# Core signal processing
numpy>=1.24
scipy>=1.11
soundfile>=0.12
librosa>=0.10          # loading/resampling + baseline STFT only; the model's
                       # STFT/ISTFT are hand-written in src/manual_stft.py

# Model
tensorflow>=2.15       # Apple Silicon: use tensorflow-macos + tensorflow-metal

# Evaluation
pystoi>=0.4
pesq>=0.0.4            # needs a C compiler on Windows; installs fine on Linux/Colab

# Plots (optional, for results/samples/*.png)
matplotlib>=3.7
